import React from 'react'

import { Text, View } from 'react-native'

import { Overlay } from 'react-native-elements'

const NotaFiscalModal = ({ Nota }) => {
  return (
    <Overlay>
      <Text>Aqui</Text>
    </Overlay>
  )
}

export default NotaFiscalModal
